<?xml version="1.0" encoding="utf-8"?>
<!-- Canonical 2x2 Small entry. Width remains below the Standard tier boundary, so launcher
     variance cannot accidentally select the denser 3x2 layout. updatePeriodMillis stays zero:
     updates continue to come only from RoutineWidgetUpdater's existing worker-driven path. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="160dp"
    android:minHeight="120dp"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:minResizeWidth="160dp"
    android:minResizeHeight="110dp"
    android:maxResizeWidth="219dp"
    android:maxResizeHeight="179dp"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:previewLayout="@layout/widget_preview_compact"
    android:previewImage="@drawable/widget_preview_compact"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:description="@string/widget_description" />
